// import React from "react";
// import { AppBar, Toolbar, Typography } from "@mui/material";

// const SubNav = ({ items }) => {
//   if (!items || items.length === 0) return null; // hide if no submenu

//   return (
//     <AppBar position="static" sx={{ backgroundColor: "#f8f8f8", color: "#000", boxShadow: "none" }}>
//       <Toolbar sx={{ minHeight: "40px !important" }}>
//         {items.map((item) => (
//           <Typography
//             key={item.text}
//             sx={{ mr: 3, cursor: "pointer" }}
//             onClick={() => console.log("SubNav clicked:", item.text)}
//           >
//             {item.text}
//           </Typography>
//         ))}
//       </Toolbar>
//     </AppBar>
//   );
// };

// export default SubNav;
